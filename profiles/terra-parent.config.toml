# Install as ~/.codex/terra-parent.config.toml and launch with:
#   codex --profile terra-parent
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"

[agents]
max_threads = 6
max_depth = 2
interrupt_message = true
